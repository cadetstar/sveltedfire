  * Convert fetchDocs to process with an array (both for the field/comparator/value form and to handle sorting and limiting)
  * Migrate the AuthContext, AuthSig, to the package
  * Convert SignedIn and SignedOut to use the auth context (make sure to handle if the context is missing)
